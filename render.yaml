services:
  - type: web
    name: faa
    env: node
    plan: free
    buildCommand: npm install && rm -rf web/node_modules web/package-lock.json && cd web && npm install && npx vite build && cd ..
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: GEMINI_API_KEY
        sync: false
      - key: DATABASE_URL
        value: postgresql://chatbd_9y0m_user:0SXNxyRLutPmuHpcAViaLOsvD4QxxzEn@dpg-d20mfpjipnbc73dejim0-a.frankfurt-postgres.render.com/chatbd_9y0m
      - key: JWT_SECRET
        generateValue: true
      - key: UPLOAD_PATH
        value: ./uploads
      - key: MAX_FILE_SIZE
        value: 10485760
      - key: CHUNK_SIZE
        value: 1000
      - key: CHUNK_OVERLAP
        value: 200
    healthCheckPath: /api/health
    autoDeploy: true

# Comentado porque já temos um banco de dados criado e configurado no Render
# databases:
#   - name: faa
#     databaseName: chatbot_militar
#     user: chatbot_user
#     plan: free
#     ipAllowList: []  # opcional - permite acesso de qualquer IP 